# 李昊宇个人站 — Codex 跨电脑交接

更新时间：2026-09-06（Asia/Shanghai）

## 给接手 Codex 的第一条指令

请先完整阅读本文件，再检查 `index.html`、`portfolio.css`、`portfolio.js` 和 `nav-overrides.css`。这是一个已经反复视觉校准的现有项目，不要重建页面、替换现有素材或擅自改变信息架构。继续修改时必须以用户给出的 Figma 节点和截图为准，并至少在 1440 × 900（笔记本 100% 缩放）及 1920 × 1080 桌面视口做浏览器验证。

## 项目启动

本项目是静态网站，不需要构建主页面。

```bash
cd v-a
python3 -m http.server 4174 --bind 127.0.0.1
```

然后打开：

```text
http://127.0.0.1:4174/?mckp-cover=baseline#top
```

如果浏览器仍显示旧样式，请在查询参数中增加新的 `rev` 值，并同步更新 `index.html` 内修改过的 CSS/JS 资源版本号。

## 2026-09-05 接管与部署状态

- 项目已从 `v-a-codex-handoff-20260904.zip` 恢复到当前工作区，静态主站可通过上述命令启动。
- 本地部署已验证：`http://127.0.0.1:4174/?mckp-cover=baseline#top` 可正常加载，主页面不需要构建步骤。
- 压缩包内没有 Git 仓库信息、云平台配置、生产域名或发布凭据，因此尚未执行外部上线；确定发布平台与域名后再补生产发布。
- 2026-09-05 已完成开屏跳帧柔化、顶部 tab 悬浮恢复、二维码定位、简历联系方式和笔记本 100% 缩放适配，并在 1440 × 900 与 1920 × 1080 视口完成运行验证。

## 当前关键文件

- `index.html`：主站结构、弹窗、简历、目录、联系页
- `portfolio.css`：主站全部视觉与响应式样式，文件后半段包含多轮精确覆盖规则，修改前先搜索同名选择器
- `portfolio.js`：MCKP 开屏、滚动叙事、项目卡片、Learn More 弹窗、联系区交互
- `nav-overrides.css`：底部导航稳定版选中态
- `case.html`、`case-gallery.css`、`case-gallery.js`：精品就业班、智能音箱、悟牛共用的项目长页
- `seal.html`、`styles.css`：海豹原互动项目页、PDF补充内容和全页固定的返回目录栏
- `assets/portfolio/cases/`：从用户PDF按指定页码导出的49张项目原稿图片
- `assets/portfolio/`：封面、简历、目录、联系页、二维码和 MCKP 资源
- `assets/portfolio/mckp/scene.json`：开屏 3D 模型配置
- `assets/portfolio/mckp/mckp-player.js`：本地 MCKP 播放器运行时，除非确认根因，否则不要直接改压缩后的运行时代码
- `assets/portfolio/lanyard-island/`：当前简历证件卡运行资源
- `lanyard-island/src/`：证件绳/卡片的 React 源码；依赖未打进压缩包，需在该目录重新安装

## 最新进度

### 1. 开屏跳帧与灯光（2026-09-05 已按最新反馈调整）

用户最终确认希望保留黑色渐隐语言，但不能再有“闪一下”的感受；当前方案改为连续亮度曲线，不使用模糊柔化版本。

本次处理：

- `.is-cut-masked` 约 `2200ms` 开始、`3160ms` 结束；由 `mockup-cut-blend` 在 960ms 内完成缓慢压暗、短暂遮挡跳帧、平滑恢复，峰值黑度降至 62%。
- 扫光与屏幕翻开阶段保持原速度；遮罩结束后仅将“视角回正”阶段按基础播放速度的 `1.25×` 运行，入场完成后恢复基础速度，避免影响后续交互。
- 浏览器逐段采样得到 `0.01 → 0.063 → 0.186 → 0.335 → 0.474 → 0.578 → 0.617 → 0.62 → 0.595 → 0.523 → 0.381 → 0.229 → 0.104 → 0.021`，亮度连续变化，无透明度跳变。
- `scene.json` 中原红色主聚光灯 `#FF0000` 已替换为站点现有蓝色 `#7467FA`；通过资源版本 `scene.json?v=20260905-1` 刷新缓存。
- 蓝色封面视频达到 `loadeddata/canplay` 后才允许封面接管；资源失败时释放阻塞，避免页面卡死。
- 保留现有滚动驱动的 MCKP 渐隐与蓝色封面渐显关系，没有更改素材与信息架构。
- 自动化验证确认遮罩动画结束后类被移除、伪元素透明度回到 0、开屏状态完成；未发现页面脚本错误或资源加载错误。

相关代码位置：`portfolio.js` 的 `startMockupIntro()`、`prepareCoverBackgroundVideo()`、`updateHeroStory()`。

### 2. 顶部 tab、联系方式与笔记本布局（2026-09-05 已修复）

- 恢复顶部 tab 原有的移动悬浮胶囊；活动层改为近乎不透明黑色，悬浮/选中时不会透出底部灰色标签文字。
- 顶部 tab 图标已按 Figma 节点 `301:389` 的轮廓和两态规则对齐：未选中统一为70%透明度，选中为100%；移除原先无效的亮度滤镜，简历、项目、联系三种选中态均已实测。联系图标已从Figma完整图标组下载并整理为单一 `contact.svg`，外框和省略号不再分层定位，始终作为一个整体等比例缩放。
- 简历页 EMAIL / WECHAT 在桌面宽度内左右展开，不再挤在一处；最大占用宽度 760px。
- 联系页二维码按自身高度计算位置，固定在微信图标正上方并保持 12px 间隔。
- 为宽度大于 1180px、可用高度不超过 900px 的笔记本视口增加独立适配：压缩简历与联系区的字号、纵向间距、图片占比，不依赖浏览器缩放。
- 1366 × 768 与 1440 × 900 下，简历联系方式、项目 CTA、联系目录和页脚均完整落在视口内；1920 × 1080 原桌面比例保持不变。

### 3. 模块分界过渡（2026-09-06 已调整）

- “简历 → 项目”复用开屏进入简历的蓝黑羽化语言；“项目 → 联系”保留同样的柔和向上弥散动效，但进入联系页时使用纯黑雾层，不把目录页蓝色带入联系页。
- 分界静态羽化不再经过纯黑：从上方背景的冷蓝黑 `#05122A` 过渡到深海军蓝 `#020A1B`，再衔接下一模块的 `#00000B`；透明首尾让上下背景自然混合。
- 后一模块接近视口时，冷蓝黑渐变随滚动从下向上弥漫；过渡层跨越分界线上下，避免被模块 `overflow` 裁切成横向硬线。
- 根据 2026-09-06 的两张边界截图，移动遮罩已移除底部 52% 的纯色平台，改为 `0.78 → 0.64 → 0.46 → 0.24 → 0` 的连续透明度；静态接缝高度由最高 300px 收窄到 230px，中心峰值透明度降至 0.58。
- 针对仍可见的水平裁切线，移动遮罩最终改为两端都完全透明：`0 → 0.44 → 0.58 → 0.32 → 0`；真正的章节边界由 140–190px 的窄羽化带覆盖，仅中心 48%–52% 使用 `#020817`，不会形成大面积纯色夹层。
- 最终定位到简历→目录的剩余硬线来自目录页自身顶部：`.projects-section::after` 原本从第一像素覆盖 `#00000B`。现改为顶部继承简历底色 `#05122A`，并在目录内部经过 `#040F23 → #020818 → rgba(0,1,15,.56) → transparent` 渐隐；该处理与封面→简历的“前景压暗、下一背景柔和进入”逻辑一致。
- 2026-09-06 在此基础上补齐真正的滚动动效：简历内容随边界接近逐渐淡出并轻微上移，蓝黑径向雾层从下向上穿过边界，目录标题与四张卡片再逐渐显现。边界羽化中心已对准真实分界点，不再只是静态颜色衔接。
- 主站3D尚未就绪时保留用户确认的线框原型扫光；轮廓已按当前开屏中的笔记本形状重画为屏幕、转轴、键盘底座和触控板，不再显示旧的手机轮廓或品牌字标。加载失败页仍保留明确重试按钮。
- 实测滚动过程中透明度连续经过约 `0 → 0.14 → 0.41 → 0.71 → 0.94`，不是静态阴影或瞬间切换。

### 4. PDF项目页与目录入口（2026-09-06 已接入）

- 用户PDF共54页、1920×1080基准，页面为图像内容；按用户指定范围导出为网页图片并保留每页原始宽高比例。
- 2026-09-06 已使用用户重新提供的未压缩高清版 `protfolio.pdf`（约77MB）重导全部49张网页图片；页码映射与内容不变，输出宽度由1920提升到2560，JPEG质量92，四个目录数量仍为21 / 8 / 6 / 14。
- 01 精品就业班：PDF 4–24页，共21页，入口 `case.html?project=employment`。
- 02 海豹上岸游记：保留原 `seal.html` 互动内容，并在页面末尾补充PDF 25–32页，共8页；“返回项目目录”栏固定在视口顶部，从原互动内容到PDF补充内容全程保留。
- 03 智能音箱：PDF 33–38页，共6页，入口 `case.html?project=speaker`。
- 04 悟牛 APP：PDF 39–52页，共14页，入口 `case.html?project=wuniu`。
- 四张目录卡均已改为 `live`，逐张鼠标点击实测路由和页面数量正确；项目长页提供返回目录、当前页码和阅读进度。
- 四个项目页顶部统一保留桌面约133px、移动端80px的导航空间。精品就业班与悟牛使用纯 `#F2F4F7`，智能音箱保留浅天蓝，海豹使用与首屏一致的浅灰白。
- 三个通用项目页的顶部空间在真实分界点前保持清晰，首图只从分界点下方开始渐显，并在112px内恢复清晰；接触区仅叠加4px轻模糊。精品就业班与悟牛的分界点上方均不再出现图片、模糊或有色背景。
- 目录页不再支持拖拽、滑动或方向键切换；四张卡固定保持当前排布，只保留鼠标悬浮散开和单击进入项目。
- 修复直接以 `#work` 打开时入场观察偶发失效、四张卡永久停在屏幕外的问题；直接进入目录时会立即显示卡片，正常向下滚动仍保留原入场动效。

### 5. 简历个人卡（已恢复原版）

- Figma `293:321` 的正面、背面与挂绳曾做过本地适配试验，用户随后明确要求“个人卡恢复之前版本”。
- 当前线上源码已恢复原照片正面、作品集封面背面、紫色挂绳及原有摆动/拖拽物理效果；不要再次自动替换为 Figma 卡。
- 改动前可回退备份：`outputs/v-a-before-figma-card-20260906.zip`。

## 已完成且不要回退的内容

- 目录页四张卡片已按 Figma 折叠关系重新排布，悟牛卡不再完全孤立；hover 保留轻微散开，不要大幅推开。
- Learn More 打开 16:9 弹窗，内部显示 `assets/portfolio/about-work-20260903.jpg` 并上下滚动。
- 鼠标在弹窗内部滚轮只控制弹窗；鼠标在外部滚动会关闭弹窗并恢复页面。
- 简历与目录背景已替换，三个主要章节使用同方向的冷蓝黑向上弥漫过渡。
- 顶部导航的移动悬浮胶囊已恢复；活动层使用近乎不透明黑色，确保图标与文字为白色且不透出下方灰字。
- 联系页的信息层级已按国内阅读习惯调整：主要信息在上，英文类型标签在下。
- 微信二维码已放大，并精确定位在微信图标上方 12px。
- 已移除开屏中的 `2023–2026` 页面层文字和 `LEE · UI / AI DESIGNER` 页面层文字；如果仍在 MCKP 屏幕视频中看到，那是视频内容本身，不是 DOM 文案。
- 开屏跳帧使用 960ms 连续亮度曲线，峰值黑度 62%，不再使用短促闪黑；蓝色封面视频首帧就绪后再参与交叉渐显。
- 开屏主聚光灯已从红色改为 `#7467FA` 蓝色。
- 目录四张封面均已接通对应项目，禁止重新改回“正在整理中”。

## 设计稿与视觉基准

- 4:3 / 16:9 布局参考：<https://www.figma.com/design/n8OAj2W7WTEmhQQroX2VzA/%E4%B8%AA%E7%AB%99?node-id=274-96045>
- 简历与目录修改：<https://www.figma.com/design/n8OAj2W7WTEmhQQroX2VzA/%E4%B8%AA%E7%AB%99?node-id=274-97554>
- 结尾信息层级：<https://www.figma.com/design/n8OAj2W7WTEmhQQroX2VzA/%E4%B8%AA%E7%AB%99?node-id=274-98355>
- 联系页与二维码：<https://www.figma.com/design/n8OAj2W7WTEmhQQroX2VzA/%E4%B8%AA%E7%AB%99?node-id=278-374>

目录页必须保持以下关系：四张卡片形成一个连续组合，01 与 02 交叠、02 与 03 交叠、03 与 04 保持较小但清晰的视觉关联；整体需要有呼吸感，不能把四张卡平均挤成一排。

## 当前资源版本

当前 `index.html` 使用：

- `portfolio.css?v=20260906-11`
- `nav-overrides.css?v=20260906-5`
- `portfolio.js?v=20260906-7`
- `assets/portfolio/lanyard-island/lanyard.js?v=20260906-4`
- `case-gallery.css?v=20260906-8`
- `case-gallery.js?v=20260906-2`
- `styles.css?v=20260906-7`（由 `seal.html` 引用）
- `assets/portfolio/mckp/scene.json?v=20260905-1`（由 `portfolio.js` 引用）

修改这些文件后请递增对应版本号，以避免另一台电脑的浏览器缓存误判为“没有生效”。

## 2026-09-06 运行验证结果

- 1366 × 768、1440 × 900（笔记本 100%）：简历、四卡目录、联系页、二维码、导航悬浮和章节过渡通过；项目 CTA 与联系页页脚均未溢出。
- 1920 × 1080：简历定位、四卡目录、导航悬浮/选中态和联系页布局通过，未因笔记本适配发生回退。
- 开屏：960ms 遮罩曲线逐段采样连续，峰值黑度 62%，动画结束后透明度恢复为 0；主聚光灯配置确认是 `#7467FA`。
- 简历→目录：在用户截图对应的 `scrollY≈2109` 复测；目录顶部已从 `#05122A` 连续渐入内部背景，未见贯穿页面的水平线。
- 简历→目录动效：中间帧实测简历退出进度0.87、雾层透明度0.82、目录进入进度0.46；最终目录进入进度1，四张卡片全部可见。
- 首次加载页：实测在 `mockup-shell is-loading` 阶段显示与当前笔记本封面一致的线框轮廓和扫光；模型就绪后正常淡出。
- 开屏速度：扫光和翻屏维持原速，`3160ms` 后的视角回正阶段按基础速度 `1.25×` 播放，结束后恢复基础速度。
- 简历个人卡：已按用户最新要求恢复原版照片卡、作品集背面和紫色挂绳，画布可见且保留摆动/拖拽。
- 项目入口：四张封面逐张鼠标点击通过，分别加载21、8、6、14页；首张资源对应PDF第4、25、33、39页。
- 高清项目图：49张图片均核对为2560px宽，页码范围与文件名连续；浏览器实测精品就业班首图2560×2160、音箱2560×5379、悟牛2560×1440、海豹2560×2054，控制台无资源错误。
- 项目页顶部：海豹与通用项目页内容起点均实测为约133px；固定导航均为约620px宽、58px高，在1330px视口下距顶约32px。精品就业班顶部已实测为 `rgb(242, 244, 247)`。
- 目录交互：拖拽、滑动和方向键切换已全部移除；直接打开 `#work` 时四张卡立即可见，悬浮散开仍正常。真实鼠标点击第一张卡约 0.93 秒完成黑色转场并进入项目页，控制台无 error。
- 海豹项目页：返回目录栏在原互动页与PDF补充区均为 `fixed`；其桌面顶部定位与通用项目页统一使用 `clamp(16px, 2.5vw, 32px)`，移动端为16px。
- 海豹项目页：原第04章“元素拆解，道具设定”已按用户要求删除，原第05章“激励延伸”顺延为04；六个场景卡片内部编号保持01–06不变。
- 海豹原项目内容与PDF补充内容之间的底部白色留白，在桌面和移动端均增加20px。
- Learn More：弹窗比例实测为 1.778（16:9）；内部滚轮只滚弹窗，外部滚轮关闭弹窗并恢复页面。
- 联系页：微信二维码显示在微信图标上方，尺寸 140 × 148、间隔 12px；联系 tab 图标与文字为白色选中态，底层灰字不可见。
- 顶部导航图标：分别直接进入 `#profile`、`#work`、`#contact` 验证，活动索引依次为0、1、2；选中图标计算透明度均为1，默认层三个图标计算透明度均为0.7。
- 联系图标对齐：浏览器实测选中层与默认层均使用同一完整 `contact.svg`，旧外框/省略号子图均为隐藏状态；选中整体透明度为1，默认整体透明度为0.7。
- 控制台：无 error。仍有两个既有 warning：Three.js 多实例提示，以及证件卡运行时初始化参数弃用提示；本次未修改第三方/打包运行时代码。
- 若在同一浏览器标签中连续大量重载3D开屏与证件卡，可能出现 `WebAssembly.instantiate(): Out of memory` 和 WebGL context lost；这是测试标签累计的渲染内存问题，不是素材丢失。关闭该标签并新开主站即可释放。2026-09-06 已用全新标签复测：开屏完成、证件卡canvas可见、四张项目图完整，控制台0 error。

## 继续开发的验证顺序

1. 从 `#top` 冷启动，等待 MCKP 入场结束，检查是否闪黑、跳帧或突然缩放。
2. 下滑到简历，检查蓝色封面到简历的柔和过渡和滚动时长。
3. 到目录页检查四卡折叠关系，并逐张 hover，确认只是轻微散开。
4. 点击 Learn More，验证 16:9 弹窗、内部滚动、外部滚动关闭。
5. 到联系页检查信息间距、二维码位置和底部导航三个 tab 的白色选中图标。
6. 检查浏览器控制台无新增 error；既有 warning 需记录来源，再交付。
7. 逐张点击四个目录封面，确认分别进入精品就业班、海豹、智能音箱和悟牛内容；检查返回目录按钮与长页原始比例。

## 跨电脑进度同步建议

压缩包适合第一次迁移。之后如果两台电脑都要继续编辑，请只保留一个主版本，或把项目放进私人 Git 仓库/同步盘；每次修改前先同步，修改后再提交或覆盖。不要让两个 Codex 同时修改同一份 `portfolio.css`，因为该文件存在多轮覆盖规则，直接合并非常容易产生视觉回退。
